type Session @model @auth(rules: [{allow: owner}, {allow: private}]) {
  id: ID!
  DateTime: AWSDateTime
  Description: String
}

enum AccountType {
  SUPER_ADMIN
  USER
  SUPERVISOR
  THERAPIST
}

type UserData @model @auth(rules: [{allow: owner}, {allow: private}]) {
  id: ID!
  FirstName: String
  LastName: String
  Email: AWSEmail
  Phone: AWSPhone
  AccountType: AccountType
}
